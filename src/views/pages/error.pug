extends ../layouts/layout_default

block pageVariables
  - pageTitle = 'Error Page'
  - isPageError = true

block content
  //- .error-page class for custom styles
  section.section-error-page
    .container
      h1.title
        | Page not found.

      p.text
        | The page you are looking for does not exist.

      p.text
        | Sorry :(

      //- button go to homepage
      .row
        .col.align-self-center
          include ../components/_button_back_home.pug
